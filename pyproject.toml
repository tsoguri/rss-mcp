[project]
name = "rss-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.14.2",
    "feedparser>=6.0.12",
    "httpx>=0.28.1",
    "mcp[cli]>=1.17.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.2",
]
